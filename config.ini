[paths]
PROJECT_BASE_DIR = env/dev

[project]
project_prefix = "project"

[apps]
app_prefix = "app"
app_files = "no" ; this is for app level views, models, forms, etc., if no then will be deleted

[modules]
mod_prefix = "mod"